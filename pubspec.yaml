name: awalys
description: App educacional de notas inteligentes com análise semântica
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  flutter_colorpicker: ^1.0.3
  emoji_picker_flutter: ^2.0.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  http: ^1.1.0
  html: ^0.15.4
  uuid: ^4.2.1
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
